# Baseline RetinaNet config.
data:
  dataset_root: ./data/rdd
  image_size: 800
  batch_size: 2
  num_workers: 4

model:
  name: retinanet
  num_classes: 4
  pretrained: true

train:
  epochs: 12
  lr: 0.01
  weight_decay: 0.0001
  optimizer: sgd
  seed: 42
  amp: true